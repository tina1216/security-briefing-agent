repos:
  - repo: https://github.com/semgrep/semgrep
    rev: v1.80.0
    hooks:
      - id: semgrep
        name: Semgrep Python Security Scan
        files: \.py$
        args: ['--config', 'auto', '--error', '--skip-unknown-extensions']
        additional_dependencies: ['setuptools<82.0.0']

